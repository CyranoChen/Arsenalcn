@if (Request.IsAuthenticated)
{
    @Html.ActionLink(User.Identity.Name, "MyAvatar", "Account", null,
            new { data_icon = "gear", data_transition = "slidedown", data_ajax = "false" })
}
else
{
    @Html.ActionLink("登录", "Login", "Account", null, new { data_icon = "gear", data_transition = "slidedown" })
}